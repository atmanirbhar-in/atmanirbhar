defmodule AtmanirbharWeb.FaqsLive do
  use AtmanirbharWeb, :live_view

  def mount(assigns, socket) do
    {
      :ok,
      socket
    }
  end

  # def handle_params(params, assigns, socket) do
  # end

end
